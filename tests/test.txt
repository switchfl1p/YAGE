Shader Class tests:
-pass shader file with wrong extension
-pass shader file that doesn't exist
-pass shader file that doesn't compile

Program Class tests:
-attempt to attach a shader that didn't compile

from root:

g++ -std=c++17 -o tests/test tests/main.cpp core/Camera.cpp -I./lib/glm -I./core

from tests:

g++ -std=c++17 -o test main.cpp ../core/Camera.cpp -I../lib/glm -I../core

